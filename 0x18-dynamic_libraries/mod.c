#include "main.h"

/**
 * mod - modulus of two numbers from input
 * @a: first parameter
 * @b: second parameter
 *
 * Description: modulus of two numbers
 * Return: always (0)
 */

int mod(int a, int b)
{
	return (a % b);
}
