#include "main.h"

/**
 * mul - multiply two numbers from input
 * @a: first parameter
 * @b: second parameter
 *
 * Description: multiply two numbers
 * Return: always (0)
 */

int mul(int a, int b)
{
	return (a * b);
}
