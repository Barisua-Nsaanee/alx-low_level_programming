#include "main.h"

/**
 * add - add two numbers from input
 * @a: first parameter
 * @b: second parameter
 *
 * Descripyion: adds two numbers
 * Return: always (0)
 */

int add(int a, int b)
{
	return (a + b);
}
