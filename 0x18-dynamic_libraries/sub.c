#include "main.h"

/**
 * sub - subtract two numbers from input
 * @a: first parameter
 * @b: second parameter
 *
 * Description: subtract two numbers
 * Return: always (0)
 */

int sub(int a, int b)
{
	return (a - b);
}
