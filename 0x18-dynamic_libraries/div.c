#include "main.h"

/**
 * div - divides two numbers from input
 * @a: first parameter
 * @b: second parameter
 *
 * Description: divides two numbers
 * Return: always (0)
 */

int div(int a, int b)
{
	return (a / b);
}
